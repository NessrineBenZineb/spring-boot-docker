server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/NessrineBenZineb/myConfig
      overrideSystemProperties: false
      overrideNone: true